/**
 * Figma node 0:953 — BYREDO stencil signature (1413 × 294 px)
 * Uses the downloaded SVG vector asset matching the Figma design.
 */
export function ByredoSignature(): React.JSX.Element {
  return (
    <section className="w-full px-[10px] pt-16 pb-4 overflow-hidden mt-[100px]">
      {/* eslint-disable-next-line @next/next/no-img-element */}
      <img
        src="/assets/기타/byredo-signature.svg"
        alt="BYREDO"
        className="w-full h-auto block select-none"
        draggable={false}
      />
    </section>
  );
}
